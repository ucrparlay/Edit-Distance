#include "edit_distance_sequential.h"

#include <algorithm>
#include <vector>

size_t EditDistanceSequential::Solve(const parlay::sequence<uint32_t>& a,
                                     const parlay::sequence<uint32_t>& b) {
  return 0;
}
